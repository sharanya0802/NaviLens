# NaviLens — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Speech Recognition (always-on listening) ─────────────────────────────────
SpeechRecognition>=3.11.0   # Microphone capture + voice activity detection
pyaudio>=0.2.14             # Microphone access

# ── Whisper (local speech-to-text) ────────────────────────────────────────────
faster-whisper>=1.0.0       # CTranslate2-based Whisper (fast local STT)

# ── Text-to-Speech ────────────────────────────────────────────────────────────
pyttsx3>=2.90               # Offline TTS fallback
edge-tts>=6.1.0             # Optional: neural voice (pip + --tts-engine edge)
gTTS>=2.5.1                 # Optional online TTS
pygame>=2.6.0               # Audio playback for gTTS / edge

# ── Product identification (Gemini — products/labels only, not navigation) ───
google-genai>=1.0.0         # Gemini Vision for brand/price on packages
open-clip-torch>=2.24.0     # Local CLIP fallback when Gemini unavailable
easyocr>=1.7.0              # Label text extraction (local)
Pillow>=10.0.0

# ── Depth Estimation (local navigation) ──────────────────────────────────────
timm>=1.0.0                 # MiDaS dependency for depth model transforms
torch>=2.0.0                # PyTorch (shared by Whisper, MiDaS, YOLO)
torchvision>=0.15.0         # MiDaS transforms

# ── Object Detection (navigation-critical objects) ───────────────────────────
ultralytics>=8.0.0          # YOLOv8n for person/chair/door/stairs detection

# ── Camera & Utilities ───────────────────────────────────────────────────────
opencv-python>=4.10.0       # Frame capture, display, depth visualization
numpy>=1.26.0
